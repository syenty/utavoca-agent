# Credentials
.env
.env.local
.env.*.local
!.env.example

# Lyrics files — copyrighted content, not committed
lyrics/*.txt
lyrics/raw/*.txt
archive/*.txt

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*.swo

# Claude Code — session artifacts only (agents/ and commands/ ARE committed)
.claude/settings.local.json
